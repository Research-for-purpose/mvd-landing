# Optional on Vercel; set this when the public site uses a custom domain.
# Used for canonical metadata, robots.txt, and sitemap.xml.
NEXT_PUBLIC_SITE_URL=https://mvd.example

# Add analytics or other public configuration only after the privacy policy
# has been updated to identify the provider and its data practices.
